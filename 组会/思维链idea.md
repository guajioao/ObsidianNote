
多生成几个part的掩码，不要求大小，只要求彼此之间尽量少的相交，但是尽量让所有的区域都在GT掩码的范围内
为了尽量覆盖不留下空隙，限制至少要与一个区域有相交的地方（中间的空洞怎么办？）
如何判断生成几个part？自己学？

从一个已知的，符合要求的掩码出发，一步一步生成覆盖整个object的掩码【think step by step】

如何避免一步错，步步错的情况？ZegCLIP的patch识别方法？





ZegFormer，设置N个query，将图像分为N个语义无关的段，然后进行语义投影去和文本特征进行相似度计算分类
能否让这个N不设置为类别个数，而是让机器根据图像特征自己判断要分成几个类？就是一个类一个类的生成，然后可以同时推理好几个类【ZegCLIP中的NEL】。不过这样就涉及到不同类别重叠区域的处理的【Transformer输出序列不知道要几个，那就一个一个生成】
* 翻译问题是语音->文本，
	* 语音是固定长度，但是与文本的长度之间不存在固定关系。
	* 每个文字需要结合其他一小段语音生成【注意力机制】
* 语义分割：图像区域分类是像素->长度固定，值的取值范围为$[0,N]$，的序列，N是词汇表长度
	* 图像大小是固定的，但是分类出来的类别个数也是未知的
	* 每个像素的类别需要根据其他区域综合判断【注意力机制】
* 显著目标分割：像素->0/1序列，长度固定【图像大小HxW】
	* 


SETR
用vit提取特征，然后拼合多层特征图的语义信息
能不能一个类一个类的判断并识别呢。就是推理的时候从左上或者某个地方开始，判断当前像素（区域）属不属于这个类，然后逐类生成。