###### [[MaskCLIP]]
* [无需标注！将CLIP直接用于分隔](https://mp.weixin.qq.com/s/-9tfwmbwB0km6HwbW4GI8w)
	* 直接将CLIP的图像编码器权重加载到骨干网络，同时使用CLIP的文本编码器提取目标物体的词向量，生成分类器（效果很差）
		* 公式： ![[DeepLab_公式.png]]
			* V指骨干网络（带有空洞卷积的ResNet，权重加载自CLIP的图像编码器）
			* H指DeepLab的[[ASPP]]模块（权重随机初始化）
			* C是DeepLab的分类器
		* 表现很差，因此作者认为传统的微调方法破坏了CLIP的能力，可能的原因：
			* 骨干网络的结构不与CLIP的图像解码器完全相同（带有空洞卷积）
			*  图像解码器的权重在微调中被更新
			* M仅参与已见类别的训练
	* 改进：避免引入新的参数，尽量保持CLIP原有的空间
		* 将CLIP的全局注意力池化层AttnPool直接用F层替代,输出特征图而非全局归纳向量
			* 全局注意力池化层公式： ![[MaskCLIP_全局注意力池化层公式.png]]
			* Emb()和F()为线性映射层
			* AttenPool()实际上是F(vi)的加权平均数，其输出代表了整个图像的特征
			* 因此作者认为F已经蕴涵了局部语义信息，并与词向量关联，于是直接用F替代AttenPool来输出特征图，而不是原有的全局归纳向量
	* MaskCLIP+
		* 将MaskCLIP的预测用作伪标签，训练更适合分割任务的模型（例如DeepLab）
		* 当MaskCLIP+训练一段时间后性能超过MaskCLIP后，直接使用MaskCLIP+为自己提供伪标签，即自学习

###### Zero-shot Referring Image Segmentation with Global-Local Context Features
* [向CLIP学习预训练跨模态！简单高效的零样本参考图像分割方法](https://mp.weixin.qq.com/s/LR6JowZkcxWG1qTn_-ba5g?poc_token=HCdSEWWjHY1jIWRpnjcXlyqZt2_itqb4XvV26hFi)
	* [代码](https://github.com/Seonghoon-Yu/Zero-shot-RIS)
	* note:[[Zero-shot RIS with Global-Local Features]]
	* Referring image segmentation：给定一个描述文本，找到描述的区域
		* 非常具有挑战性的任务
		* 非常依赖注释
		* 与单纯的语义分割任务不同：
			* 需要对语言的高层次理解和对图像的全面理解
			* 语义分割任务不需要区分实例，而Referring需要预测实例级分割掩码
			* 在同一个类的多个实例中，只能选择表达式描述的一个实例，如对于文本“一只猫躺在座位上的踏板车”，需要了解对象之间的关系（即“躺在座位上”）及其语义（即“猫”、“滑板车”）
	* 为了解决上述两个问题，使用CLIP的零样本迁移
		* 为了正确定位图像中的物体提出mask-guided visual encoder
		* **Mask-guided Global-local Visual Features**
			* **Global-context Visual Features**：提取屏蔽区域周围信息的特征，以理解多个对象之间的关系
			* **Local-context Visual Features**：在给定掩码建议的情况下获得局部上下文视觉特征（？）
			* **Global-local Context Visual features**：融合上面两个特征
		* **Global-local Textual Features**
			* 与上面的Visual类似
			* 问题：CLIP 文本编码器可以提取与图像级表示对齐的文本表示，但很难将注意力集中在表达式中的**目标名词**上，因为该任务的表达式是由包含多个从句的**复杂句子**构成的
			* 解决方案：利用spaCy找到目标名词短语

###### Open-Vocabulary Semantic Segmentation with Decoupled One-Pass Network
* [只需过一次CLIP！美团&港大提出DeOP：高效的开放字典语义分割框架](https://mp.weixin.qq.com/s/kW2uq1J9I7SwKGzEyb5pBw)
	* [代码](https://github.com/CongHan0808/DeOP)

###### Side Adapter Network for Open-Vocabulary Semantic Segmentation
* [华科&MSRA新作：基于CLIP的轻量级开放词汇语义分割架构](https://mp.weixin.qq.com/s/KkElI1lL9TDgfZ-A1Pcp8Q)
