
* [[Zero-shot RIS with Global-Local Features]]
* [[ZegCLIP]]


CoT:思维链
[Chain-of-Thought Prompting Elicits Reasoning in Large Language Models](https://link.zhihu.com/?target=https%3A//arxiv.org/abs/2005.14165)
在问题的结尾附加prompt "think step by step"，让其尽可能生成一些思考过程，再将生成的理由和question拼在一起，再配合一个prompt "the answer is"输出答案
## 多数投票提高CoT性能——自洽性（Self-consistency）

 [Self-Consistency Improves Chain of Thought Reasoning in Language Models.](https://link.zhihu.com/?target=https%3A//arxiv.org/abs/2203.11171)


## YOLOv1
![[Pasted image 20231011194237.png]]
* 输入一张图片，将其转变为指定大小的正方形
* 将图片分为S * S个grid cell
* 每一个grid cell预测B个框(bounding box)和一个类别
* 每个bounding box包含5个参数，且bounding box的中心一定落在对应的grid cell中
* 共S * S * B = 7 * 7 * 2 = 98个bounding box ![[Pasted image 20231011194304.png]]
* 每一个gird cell需要预测出一个类别
* 进行非极大值抑制(NMS)得出最终结果7 x 7 x 30
### 输出结果
![[Pasted image 20231011195030.png]]
7 * 7大小的特征图，为7 * 7 个grid cell
30维度，前面2个5维度每个5维记录了这个cell的x,y,w,h,c五个信息，一共有2个bbox
![[Pasted image 20231011195855.png]]
用每个bbox的置信度与类别概率相乘，得到每个bbox的class scores

### 损失函数
![[Pasted image 20231011200149.png]]
* ![[Pasted image 20231011200208.png]]代表object是否出现在第i个cell里，
* ![[Pasted image 20231011200251.png]]代表第i个cell的第j个bbox预测器给出的这个预测结果


### QA：为什么Fast-RCNN背景错误多于YOLO？
* 通过selective search来生成2k个框，而yolo是通过划分grid cell，每个cell给出两个bbox，生成在背景的bbox比Fast R-CNN少
* YOLO对于正确定位物体比较苦手，Loc错误比所有其他错误加起来还高
* Fast R-CNN定位错误少的多，但是有更多background错误（认为有object，但实际上只是background）
* 原因：yolo是一个一阶段的模型，一次完成定位、分类两个任务，对于定位任务就没有专门的detedtor精通
* 而Fast R-CNN先做分类，再做定位

## SSD

在不同特征图上用卷积的方式对每个默认框预测两组值，一组表示位置，一组表示类别概率

***RPN***全称Region proposal network，作用是为第二阶段提供高质量的目标候选框
### Predictor的实现
？
### Matching strategy
jaccard overlap？

### Hard negative mining

SSD：

1. 引入了一种单阶段的检测器，比以前的算法YOLO更准更快，并没有使用RPN和Pooling操作；
***RPN***全称Region proposal network，作用是为第二阶段提供高质量的目标候选框
2. 使用一个小卷积滤波器应用在不同的feature map层从而预测BB的类别的BB偏差；
3. 可以在更小的输入图片中得到更好的检测结果（相比Faster-RCNN）；
4. 在多个数据集（PASCAL、VOC、COCO、ILSVRC）上面的测试结果表明，他可以获得更高的mAp值；

