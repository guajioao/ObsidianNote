# 不需要训练模型的方法
#### Diffusion Model is Secretly a Training-free Open Vocabulary Semantic Segmenter
* 特点：无监督，无训练
* 方法总结
	* 结构图
		* ![[Pasted image 20240115160951.png]]
	* 图像通过正常加噪过程变为$z_t$，通过BLIP获得图像的Caption并通过文本编码器提取为文本特征
	* 图像与文本"bus++ parked"做cross attention，融合每一层的交叉注意力结果
		* ![[Pasted image 20240115162256.png]]
		* 不同层的注意力信息关注点不同、噪声程度不同，融合全部交叉注意力图能更好的得到完整正确的注意力图
	* 图像与自己做self-attention，只融合最大分辨率的两个自注意力图
		* 交叉注意力层能够定位，但缺乏清晰的边界，内部也可能出现空洞
		* 只用了最大的两个自注意力图，原因作者没有详细讲，但从结果来看是要更好的
		* ![[Pasted image 20240115163256.png]]
	* 用组注意力图补全交叉注意力图，获得语义Mask
		* ![[Pasted image 20240115162607.png]]
		* 
* 创新之处
	* 用（图像与文本的）交叉注意力图定位目标物体；用自注意力图补全前者缺失的边界和内部空洞
* 可借鉴的点
	* 定位目标物体的方式(cross attention)与(self-atention)补全内部和边缘的手段
	* 缺陷：只有注意力图的情况下难以避免的会出现误关注其他信息的情况，比如“train”关注了站牌和轨道
		* ![[Pasted image 20240115163111.png]]

##### Diffusion Models for Zero-Shot Open-Vocabulary Segmentation
* 特点：开放词汇表，DM生成相同类别图像后聚类，无需训练
* 方法总结
	* 认为具有相似captions的图像视觉上常常是不同的，因此不能直接将图像与文本进行比较
	* 结构图
		* ![[Pasted image 20240112085209.png]]
	* 分为两个阶段：
	* prototype sampling原型采样阶段
		* 特征提取器使用SD+DINO+CLIP
		* 以"cat"和"dog"为query举例。先使用扩散模型分别生成一组猫的图片，一组狗的图片，再分别用特征提取器和segmenter进行处理，得到instance-level prototypes实例级原型，再通过实例原型平均加权的方法获得class-level原型
			* segmenter：无监督实例分割方法，比如CutLER，产生多个二值掩码。选择average attribution最高的作为前景；最低的作为背景
		* 通过Prototype aggregation原型聚集（K-Means聚类方法），形成正负原型，正原型即类别；负原型即背景。从猫狗的图像中提取的特征将聚集为猫原型、猫背景原型、狗原型和狗背景原型
	* 分割阶段
		* 输入的图像将被提取为特征然后与原型比较，得到推理mask
		* CLIP过滤器基于整张图像的内容过滤掉不相关的原型
* 创新之处
	* 从大量DM生成的图像提取类别原型和背景原型，输入图像的特征与这些原型进行比较
* 可借鉴的点
	* 若使用无监督分割方法，可以采用类似的与聚类中心对比相似度的思想【SwAV？】

# 生成数据+训练模型
#### Open-Vocabulary Panoptic Segmentation with Text-to-Image Diffusion Models
ODISE: **O**pen-vocabulary **DI**ffusion-based panoptic **SE**gmentation
* 特点：同时利用大规模文本-图像扩散模型和分类模型
* 方法总结
	* 结构图
	* 使用的并非DDPM，去噪过程中有**注意力**模块
		* 类似这篇：
			* ![[Pasted image 20240115210410.png]]
	* 训练过程![[Pasted image 20240115203203.png]]
		* 图像编码器参数冻结，在后面接一个MLP，输出作为隐式文本嵌入指导Unet的去噪过程【加在Unet的每一层的Cross Attention里】
		* Mask Generator：用Unet提取出的特征训练，同时输出掩码和掩码embedding【类似MaskFormer】
		* mask embedding与文本embedding计算点积来分类这些掩码embedding，掩码分类的相似度矩阵由交叉熵损失或GT损失来监督
		* 需要训练的组件：
			1. Implicit Captioner内的MLP;
			2. Mask Generator
	* 推理过程
		* ![[Pasted image 20240115205321.png]]
		* 两个损失：mask embedding与文本特征的相似度损失；mask 与原图相乘之后与文本特征的相似度损失。二者取几何平均
* 创新之处
	* Mask embedding**直接**与文本embedding做相似度计算【之前的方法】
	* 推理的时候mask embeddings和masked images特征都与文本特征做相似度计算，综合二者的分类结果
	* 
* 可借鉴的点
	* 通过将**CLIP图像编码器**提取的文本特征加入到SD的UNet中，使得用UNet特征生成的Mask与CLIP文本编码器提取的特征相关，实现Mask的开放词汇分类
	* 通过注意力模块将CLIP特征加入Unet【是否能只加入h-space？用一个更复杂的$f_t$使其能通过传入的（CLIP文本特征；时间t；h）生成$\Delta h_t$，就不用一个属性训练一个$f_t$了，而是开放的描述】

#### Dataset diffusion_ Diffusion-based synthetic dataset generation for pixel-level semantic segmentation
* 特点：生成图像同时生成对应的分割map
* 方法总结
	* 结构图
		* ![[Pasted image 20240115213938.png]]
	* 对于给定的类别，使用LLM生成或者直接使用VOC/COCO的Captions
	* 用SD生成符合描述的图像，同时利用在这个过程中产生的自注意力图和交叉注意力图生成精细的交叉注意力图作为伪标签
	* 交叉注意力使用的文本特征只有label特征不是整句的特征
	* 生成的图像用于自监督语义分割训练，语义分割器生成的mask与伪标签计算损失【+0.7%】
	* Uncertainty-aware cross-entropy loss设计了一个$(\alpha,\beta)$的阈值，预测值在这个区间内表示预测的是不确定的mask。最终计算损失的时候忽略这些来自不确定区域的损失【提高1%】
* 创新之处
	* 结合多个labels生成Caption，让SD生成复杂图片
	* 使用注意力生成掩码
	* 避开不确定区域计算损失
* 可借鉴的点
	* 避开不确定区域计算损失这个设计与ZegCLIP使用的NEL非互斥损失是否有相似的作用？避免不确定的区域损失回传或许
	* 生成图像时利用其注意力图生成掩码

##### From Text to Mask_ Localizing Entities Using the Attention of Text-to-Image Diffusion Models
* 特点：无需重新训练，可推广到定制模型上
* 方法总结
	* 结构图
		* ![[Pasted image 20240112103347.png]]
	* 先加噪，再用Unet去噪。
	* Unet中有注意力模块，联合交叉注意力与自注意力【联合方式：自注意力点乘交叉注意力】
	* 比较不同的关系图【？】+CRF后处理
* 创新之处
	* attention生成伪标签
	* 缺点：只有注意力图，存在多噪声和边缘不明确的问题
* 可借鉴的点
	* 暂无？只有注意力

##### FOSSIL_ Free Open-Vocabulary Semantic Segmentation Through Synthetic References Retrieval
* 特点：无需训练？
* 方法总结
	* 结构图
		* ![[Pasted image 20240116084054.png]]
	* 基于另一篇[[现有论文方法#ARXIV_What the DAAM_ Interpreting Stable Diffusion Using Cross Attention|DAAM]]计算Unet的注意力图，从而在图像生成的同时产生掩码。用掩码**mask**提取出的生成图像的**视觉特征**，用文本编码器提取类别标签的文本特征。二者都存储起来
	* 推理时提取所有类别的文本特征，再与训练阶段存起来的文本特征、视觉特征比对检索。检索到的文本特征对应的所有**视觉特征**用于创建多个原型，K-Means聚类这些原型得到聚类中心
		* 创建原型：文本编码为特征，并检索字典中N个最相似的文本特征
	* 待推理图形分别提取特征和使用OpenCut制作掩码，在Region pooling后每个区域的特征与聚类中心对比相似度
		* OpenCut利用了NCut方法【最小化子图方法将无向连通图分成两个不相交的子图】和DINO自注意力图自发的得到无类别掩码特点设计了掩码获得方式
* 创新之处
	* 创建特征字典，在推理的时候用视觉特征聚类中心来与对应区域的视觉特征匹配获得mask分类【问题：为什么不直接在训练阶段就存聚类中心？而是在推理阶段才计算Kmeans】
	* SD生成的图像提取的特征存下来，在推理的时候用。直接对比图像与图像特征，文本与文本特征【训练的时候知道文本与图像特征的对应，那只要query图像特征与key图像特征匹配上了，自然就知道key图像特征对应的文本特征了】
* 可借鉴的点
	* 直接用SD生成简单图像，比如背景全黑全白，此时无监督分割可以得到比较正确的掩码，再提取特征聚类？比DAAM方法得到的应该要好？【但这样Background和复杂图像还需要进一步处理】
	* 

# 可借鉴到分割任务的方法

#### ARXIV_What the DAAM_ Interpreting Stable Diffusion Using Cross Attention
* 特点:提出一个新的方法DAAM计算每个单词的attribution map
* 方法总结
	* 提出一个注意力分数图的计算方式DDAM
		* ![[Pasted image 20240116085239.png]]
	* 
* 创新之处
	* 
* 可借鉴的点



##### Text-to-Image Diffusion Models are Zero-Shot Classifiers
* 特点：
* 方法总结
	* 结构图
		* ![[Pasted image 20240112103616.png]]
		* 计算以不同标签作为去噪条件计算去噪分数【？】
* 创新之处
	* 
* 可借鉴的点
	* 


##### 