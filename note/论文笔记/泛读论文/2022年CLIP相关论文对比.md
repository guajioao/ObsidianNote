# 语义分割
## 2022
### 掩码生成
* [[MaskCLIP]]:Extract Free Dense Labels from CLIP
	* 监督学习，需要ground truth类别掩码监督训练
	* 直接对特征图进行处理（两层卷积）
	* 对图像编码器最后一层的**特征图直接卷积然后上采样**得到掩膜输出
* [[DenseCLIP]]:Language-Guided Dense Prediction with Context-Aware Prompting
	* 监督学习，需要ground truth类别掩码监督训练
	* **逐像素分类**问题
	* 特征图与文本特征计算相似度，得到**分数图**
	* 文本特征中引入视觉上下文信息prompt
* [[ZegFormer]]: Decoupling Zero-Shot Semantic Segmentation
	* 监督学习，需要GT来计算mask loss
	* 将**Zero-shot semantic segmentation** (ZS3)任务转化为**两个子任务**
		* **生成mask**：用视觉特征图与queries处理后获得**类别无关的mask**和与文本特征对齐的视觉语义分割embeddings-Semantic segment embedding【**生成mask精度提高的同时与文本特征对齐**】
		* **求mask类别**：masked图像与文本特征求相似度**获得mask的类别**【是mask还是crop，作者表示没区别】
* [[OpenSeg]]:Scaling Open-Vocabulary Image Segmentation with Image-Level Labels
	* 监督学习，需要一个类别无关的分割标注
	* **在图像特征图上进行mask**
	* 使用backbone提取特征，并使用一个**交叉注意力模块来产生N个masks**
	* 用这些mask与image特征进行Mask-based pooling，得到N个mask的图像特征
	* 与文本特征进行匹配
* [[SimSeg]]:A Simple Baseline for Open-Vocabulary Semantic Segmentation with Pre-trained Vision-language Model
	* 掩膜生成之后**mask原图**，然后输入图像编码器中提取masked image特征，最后与文本特征进行相似度计算与**分类**
### 类激活图
* [[CLIMS]]: Cross Language Image Matching for Weakly Supervised Semantic Segmentation
	* 使CAM(Class ActivationMap)中目标对象激活更完整,抑制与物体相关的背景区域
	* 通过CLIP，引入自然语言来达成上一目的
	* 弱监督
## 2023
* [[note/论文笔记/详细笔记/分割相关/2023/CESL|CESL]]，显著性检测
	* 基于[[DenseCLIP]]
	* **分数图cancate原图像特征**，再通过解码器获得cm（category-aware显著图），用cm制作masked image并分类，获得的类别分数乘以cm 来更新初始的显著图
	* 弱监督，只需要给定图像和**显著类别标签**【**减少像素级标签的标注成本**】
	* 不使用CAM
* [[SAN]]
	* 将语义分割建模为区域识别问题
	* 用SAN生成掩码建议和注意力偏移Attention Bias，ab用于掩码分类
		* ab加入sls的更新，sls与text embeddings计算相似度获得分类
		* sls被视觉token单向更新，记录视觉特征？再加入注意力偏移，变得适合掩码的分类
	* **掩码为视觉tokens和可学习的queries通过三层MLP后相乘得到**
	* 本质上还是分成两个子问题？但是从两阶段变成了一阶段
	* **不用crop的方法分类掩码**，而是用attention bias计算
	* 对[[ZegFormer]]和[[SimSeg]]的改进：
		* 从两阶段变成了一阶段
		* 改进了**mask的生成与文本语义信息无关**，模型缓慢低效的缺点
* [[ZegCLIP]]
	* 同样希望将两阶段的baseline改为一阶段的baseline
	* **不用crop图像与文本的匹配**，改为**patch特征与文本特征的匹配** 【Zegformer就是希望符合人类直觉的用proposal区域进行分类，而非pixel分类。这边取了一个折中？】
	* **Decoder中多头注意力的Scale层的输出作为掩码的来源**【这一层的输出代表Q和K之间的关联程度，与相似度计算类似？】
* [[OvSeg]]
	* 解决CLIP对masked图像识别准确度不高的问题【通过prompt】
	* 与其他涉及masked image识别的论文可以**并用**

# Referring Image Segmentation，RIS
* [[CRIS]]: CLIP-Driven Referring Image Segmentation
		* 设计视觉-语言**解码器**，来根据文本特征增多**像素级视觉特征**的细粒度语义信息
	* 将像素级视觉特征V与全局语义信息T通过对比学习，来pull文本与相关的像素特征closer，push其他无关的away

# Question:
1. CESL与ZegCLIP中用的解码器区别是什么？都生成mask，分别是如何生成mask的？
	* CESL在SOD-CLIP模块用解码器生成category-aware saliency map，用的方法不是Transformer里那个解码器，就是一个单纯的反卷积和连接concatenation。
	* ZegCLIP是用解码器多头注意力里的Scaling层的输出经过Sigmoid生成掩码，也是用到了解码器，但ZegCLIP的掩码是Decoder的中间结果，最终的输出是一个Q'
	* ZegCLIP用的方式中含有文本和patch特征的对应关系，CESL中没有
2. 直接不用masked图像，而改成用其它方法生成掩码与标签的对应关系（即SAN与ZegCLIP）效果比想办法改进CLIP对masked图像的识别准确度（OvSeg）效果更好，是OvSeg的方法不够好，改进不够明显，还是直接不用crop更好？